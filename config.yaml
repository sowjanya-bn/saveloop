# Backward-compatible aggregate config.
# Prefer the split files in config/ for new development.
objective:
  weights:
    save_rate: 0.6
    comment_rate: 0.3
    dm_rate: 0.1

thresholds:
  reel_engagement_win: 0.005
  carousel_engagement_win: 0.0055

reporting:
  summary_filename: weekly_summary.md

integrations:
  submissions_csv: data/raw/submissions.csv
